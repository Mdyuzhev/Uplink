global:
  resolve_timeout: 5m

route:
  receiver: uplink-webhook
  group_by: ['alertname']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: uplink-webhook
    webhook_configs:
      - url: 'http://uplink-botservice:7891/hooks/alerts'
        send_resolved: true
