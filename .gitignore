node_modules/
out/
dist/
*.vsix
*.log

# Секреты
.env
docker/.env

# Synapse runtime data (создаётся на сервере, не в git)
docker/synapse-data/
docker/synapse/*.signing.key
docker/synapse/*.log.config
docker/synapse/media_store/

# Claude
.claude/
Tasks/

releases/
test/

# VS Code extension builds
vscode/*.vsix
vscode/out/
vscode/webview-dist/

# Generated WASM (copied by prebuild script)
web/public/*.wasm

# Tauri
web/src-tauri/target/
web/src-tauri/gen/

# Root workspace lockfile (platform-specific optional deps break CI)
/package-lock.json
